version: "3.9"

# Production compose for DigitalOcean Droplet.
# Only nginx (port 80) is exposed publicly — all Go services communicate
# on the internal Docker network. Set ORBITRON_HMAC_KEY in .env before starting.
#
# Quick start:
#   cp .env.example .env && nano .env   # set ORBITRON_HMAC_KEY
#   docker compose -f docker/docker-compose.prod.yaml up -d --build

x-common: &common
  restart: unless-stopped

services:
  frontend:
    <<: *common
    build:
      context: ..
      dockerfile: docker/Dockerfile.frontend
      args:
        VITE_CESIUM_TOKEN: ${VITE_CESIUM_TOKEN:-}
    ports:
      - "80:80"
    depends_on:
      - groundstation
      - gs2
      - gs3
      - spacecraft
      - relay
      - relay2

  spacecraft:
    <<: *common
    build:
      context: ..
      dockerfile: docker/Dockerfile.spacecraft
    environment:
      - ORBITRON_HMAC_KEY=${ORBITRON_HMAC_KEY:?ORBITRON_HMAC_KEY must be set in .env}
      - SPACECRAFT_ADDR=:8080
      - SPACECRAFT_SCID=90
      - SPACECRAFT_VCID=0
      - SPACECRAFT_APID=256

  groundstation:
    <<: *common
    build:
      context: ..
      dockerfile: docker/Dockerfile.groundstation
    environment:
      - ORBITRON_HMAC_KEY=${ORBITRON_HMAC_KEY:?ORBITRON_HMAC_KEY must be set in .env}
      - GROUNDSTATION_ADDR=:8081
      - GS_MAX_SUBSCRIBERS=64
      - GS_LAT=-1.2864
      - GS_LON=36.8172
      - SC_ADDR=http://spacecraft:8080
      - SC_SCID=90
      - SC_VCID=0
    depends_on:
      - spacecraft

  gs2:
    <<: *common
    build:
      context: ..
      dockerfile: docker/Dockerfile.groundstation
    environment:
      - ORBITRON_HMAC_KEY=${ORBITRON_HMAC_KEY:?ORBITRON_HMAC_KEY must be set in .env}
      - GROUNDSTATION_ADDR=:8085
      - GS_MAX_SUBSCRIBERS=64
      - GS_LAT=78.2232
      - GS_LON=15.6267
      - SC_ADDR=http://spacecraft:8080
      - SC_SCID=90
      - SC_VCID=0
    depends_on:
      - spacecraft

  gs3:
    <<: *common
    build:
      context: ..
      dockerfile: docker/Dockerfile.groundstation
    environment:
      - ORBITRON_HMAC_KEY=${ORBITRON_HMAC_KEY:?ORBITRON_HMAC_KEY must be set in .env}
      - GROUNDSTATION_ADDR=:8086
      - GS_MAX_SUBSCRIBERS=64
      - GS_LAT=-53.1638
      - GS_LON=-70.9171
      - SC_ADDR=http://spacecraft:8080
      - SC_SCID=90
      - SC_VCID=0
    depends_on:
      - spacecraft

  relay:
    <<: *common
    build:
      context: ..
      dockerfile: docker/Dockerfile.relay
    environment:
      - RELAY_ADDR=:8082
      - SC1_ADDR=http://spacecraft:8080
      - GS_ADDR=http://groundstation:8081
      - GS2_ADDR=http://gs2:8085
      - GS2_LAT=78.2232
      - GS2_LON=15.6267
      - GS3_ADDR=http://gs3:8086
      - GS3_LAT=-53.1638
      - GS3_LON=-70.9171
      - RELAY_SCID=91
      - GS_LAT=-1.2864
      - GS_LON=36.8172
      - MIN_ELEV_DEG=5.0
      - POLL_INTERVAL_SEC=30
      - LINK_LOSS_RATE=${LINK_LOSS_RATE:-0.05}
    depends_on:
      - spacecraft
      - groundstation
      - gs2
      - gs3

  relay2:
    <<: *common
    build:
      context: ..
      dockerfile: docker/Dockerfile.relay2
    environment:
      - RELAY2_ADDR=:8083
      - SC1_ADDR=http://spacecraft:8080
      - RELAY1_ADDR=http://relay:8082
      - GS_ADDR=http://groundstation:8081
      - GS2_ADDR=http://gs2:8085
      - GS2_LAT=78.2232
      - GS2_LON=15.6267
      - GS3_ADDR=http://gs3:8086
      - GS3_LAT=-53.1638
      - GS3_LON=-70.9171
      - RELAY2_SCID=92
      - GS_LAT=-1.2864
      - GS_LON=36.8172
      - MIN_ELEV_DEG=5.0
      - POLL_INTERVAL_SEC=30
      - LINK_LOSS_RATE=${LINK_LOSS_RATE:-0.05}
    depends_on:
      - spacecraft
      - groundstation
      - gs2
      - gs3
      - relay
