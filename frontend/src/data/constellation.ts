// Primary spacecraft are now fully TLE-driven via /constellation endpoint.
// See primarySatellites.ts for NORAD-ID-keyed canonical config.
// Satellite-1 (SIM) removed — all primaries use real TLE data.
export {}
